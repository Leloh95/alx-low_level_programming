#include"main.h"

/**
 * int _islower - print lowercase characters
 * c - characters
 * main - start program
 * Return: 0
 */

int _islower(int c)
{

if (c >= 65 && c <= 122)
        return (1);
return (0);
}
