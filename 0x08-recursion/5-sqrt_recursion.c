#include "main.h"

/**
 *_sqrt - print out squares
 * @n: input
 * Return: 0(success)
 */

int _sqrt_recursion(int n)
{
	int sqrt = i * i;

	if (sqrt > n)
return (-1);

	if (sqrt == n)
return (i);
	
return (_sqrt(n, i + 1));
}

