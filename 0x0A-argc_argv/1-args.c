#include"main.h"
#include<stdio.h>

/**
 * main - start of program
 * @argc: counts arguments passed to it
 * @argv: counts strings
 * Return: 0(success)
 */

int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc - 1);
return (0);
}
