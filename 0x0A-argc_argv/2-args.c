#include"main.h"
#include<stdio.h>

/**
 * main - start of program
 * @argc: counts arguments passed to it
 * @argv: counts strings
 * Return: 0(success)
 */

int main(int argc, char *argv[])
{
int i = 0;

if (argc > i)
printf("%s\n", argv[i]);
i++;
return (0);
}
