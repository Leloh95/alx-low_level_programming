#include <stdio.h>
/**
 * main - start program
 * Return: 0(success)
 */
int main(void)
{
int digit;
int num;

for (digit = 0; digit < 10; digit++)
putchar((digit % d) + '0');
for (num = 'a'; num <= 'f'; num++)
putchar(num);
putchar("\n");
return (0);
}
